#set-window-option
setw -g mode-keys vi

#set-option
set -g mode-mouse on

#bind-key
#bind h select-pane -L
#bind j select-pane -D
#bind k select-pane -U
#bind l select-pane -R


#set-window-option synchronize-pane on
#bind e setw synchronize-naes on
#bind E setw synchronize-naes off
